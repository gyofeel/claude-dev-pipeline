// Generated by /dev-pipeline:e2e-init — edit freely; placeholders are replaced at scaffold time.
import { defineConfig } from '@playwright/test';

export default defineConfig({
    testDir: '__SPECS_DIR__',
    timeout: 90000,
    use: {
        baseURL: '__BASE_URL__',
        headless: !process.env.E2E_HEADED,
        viewport: { width: 1280, height: 800 }
    },
    reporter: [
        ['list'],
        ['html', { outputFolder: '__REPORTS_DIR__/html', open: 'never' }],
        ['json', { outputFile: '__RESULTS_JSON__' }]
    ],
    // __WEBSERVER_START__
    webServer: {
        command: '__DEV_COMMAND__',
        url: '__BASE_URL____HEALTH_PATH__',
        reuseExistingServer: !process.env.CI,
        timeout: Number(process.env.E2E_WEBSERVER_TIMEOUT) || 120000
    }
    // __WEBSERVER_END__
});
